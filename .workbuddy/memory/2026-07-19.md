# 2026-07-19

## 门户详情页「浏览量」改名 + 挪位
- 需求：用户想把详情页顶部的「x 次浏览」换成更好的名字和位置。
- 决定：文案改为「浏览量」（数字保持原始计数，不加格式化）；位置从顶部 `#meta` 小字行移除，挪到右侧信息栏（`PortalDetailLayout` 的 `#aside` / `PortalResultDetail` 的 `#info`）的 `<dl>` 末尾，作为一条「浏览量」。
- 涉及 7 个文件（`frontend/src/views/portal/`）：NewsDetailPage、MemberDetailPage、ResearchDetailPage、AwardDetailPage、PublicationDetailPage、ProjectDetailPage、PatentDetailPage。
- 验证：全局 grep `次浏览` = 0（已彻底清除）；`浏览量` = 7 处新增。纯模板改动，`view_count` 字段原已存在，无后端 / 类型变更。
- 范围：仅门户详情页，未动列表卡片或内部平台。若日后要在列表卡片显示浏览量，需另议。
