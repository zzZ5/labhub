# LabHub 优化推进清单

本文档用于后续持续整理 LabHub。按阶段顺序推进：当前阶段通过验收后，再进入下一阶段。除紧急缺陷外，不跨阶段混改数据库、权限和页面结构。

状态说明：

- `[ ]` 未开始
- `[-]` 进行中
- `[x]` 已完成并验证

## 总体原则

- 保留现有有效数据，删除模型或字段前必须先做数据迁移和数据库备份。
- 学校身份、课题组身份和系统权限分别表达，不能再用一个角色字段承担全部含义。
- 前端重构不改变现有 API 行为；API 调整必须先补测试，再同步前端。
- 页面列表统一支持搜索、分页、返回后保留页码和筛选条件。
- 文件统一支持上传进度、大小显示、权限校验、预览和下载。
- 每个阶段单独提交，避免形成无法审查的大型提交。

## 阶段一：整理身份、权限和毕业成员逻辑

### 目标

建立稳定、易解释的账号体系，明确“这个人在学校中的身份”“在课题组中的状态”和“能在系统中做什么”是三件不同的事。

### 任务

- [x] 盘点 `accounts`、`members`、`students` 中重复的身份字段、角色枚举和判断逻辑。
- [x] 定义学校身份：硕博导师、博士后、博士生、硕士生、本科生、其他成员。
- [x] 定义成员状态：在组、已毕业/离组；成员状态不直接等同于账号是否可登录。
- [x] 定义系统权限：系统管理员、网站编辑、资料管理员、仪器管理员；普通成员不作为系统权限。
- [x] 明确 `User.is_active` 只控制账号能否登录，不用于表达毕业或离组。
- [x] 建立毕业处理规则：成员身份转为“已毕业”，保留历史成果和学生档案，不自动删除账号。
- [x] 毕业或离组后默认保留账号和查看权限；是否允许登录由账号启用状态单独控制。
- [x] 保证一个账号最多关联一个学生档案，硕博导师和博士后不自动生成学生档案。
- [x] 保留学生与多个导师的关联；毕业后档案进入历史状态，但仍可按权限查看。
- [x] 统一后台权限判断，避免只依赖前端隐藏按钮。
- [x] 编写数据迁移，归一化旧角色、成员类型和毕业状态。
- [x] 更新成员管理页，让身份、成员状态、系统权限分区显示和编辑。

### 权限基线

| 操作 | 管理员 | 内容/资料管理员 | 普通成员 | 文件本人 |
| --- | --- | --- | --- | --- |
| 管理账号和系统权限 | 允许 | 禁止 | 禁止 | 禁止 |
| 编辑门户内容 | 允许 | 内容管理员允许 | 禁止 | 禁止 |
| 查看组内资料 | 允许 | 允许 | 允许 | 允许 |
| 编辑或删除内部资料 | 允许 | 资料管理员允许 | 禁止 | 无资料管理权限时禁止 |
| 查看学生档案及归档资料 | 允许 | 已审核组内成员均允许 | 已审核组内成员均允许 | 允许 |
| 删除学生档案或文件 | 允许 | 禁止 | 禁止 | 仅本人文件允许 |

### 验收标准

- [x] 页面能够明确区分学校身份、成员状态和系统权限。
- [x] 毕业不会造成成员资料、成果署名和学生档案丢失。
- [x] 停用账号后无法登录，但公开团队成员资料可按排序继续展示。
- [x] 普通成员无法通过直接调用 API 越权管理账号或删除他人文件。
- [x] 旧数据迁移后不存在一个账号关联多个学生档案的情况。

完成记录（2026-07-16）：迁移前已备份开发数据库；应用 `accounts.0007`、`accounts.0008` 和 `students.0011` 后保留现有账号身份、管理员权限、学生档案和归档资料。学校身份收敛为六类，毕业和离组合并，成员管理与学生档案共用账号头像；学生档案及归档资料统一为已审核组内成员可查看。完整后端测试 62 项通过，前端生产构建和 Django 系统检查通过。

## 阶段二：精简仪器与内部资料后端

### 目标

让后端模型与实际实验室工作方式一致，去掉预约、培训、版本等已经明确不需要的复杂设计，同时保留图片、说明、预览和权限能力。

### 仪器平台

- [x] 保留核心字段：名称、型号、状态、详细位置、负责人、设备图片、使用说明、排序。
- [x] 核对并删除未使用的预约、培训记录、培训状态、故障上报、二维码和复杂维护台账接口。
- [x] 状态只保留实际需要的选项，例如正常、维护中、停用。
- [x] 保留批量导入、搜索、分页、详情、编辑和删除。
- [x] 删除旧字段前迁移已有图片、负责人、位置和使用说明。
- [x] 清理 Django Admin、序列化器、URL、前端类型和导入模板中的旧字段。

### 内部资料

- [x] 将“资料”和“当前文件”的关系简化为用户可理解的一份资料对应一个当前文件。
- [x] 前端不再显示版本号和更新说明；评估是否将底层 `DocumentVersion` 收敛为内部文件记录。
- [x] 若删除多版本模型，先将每份资料的当前文件、预览 PDF、原文件名、文件大小和上传人迁移到新结构。
- [x] 保留分类、标题、简短说明、上传人、上传时间、文件大小、预览状态和下载权限。
- [x] 默认组内成员可见；管理员和上传者可编辑、删除，其他普通成员只读。
- [x] 保留 Word/PPT 转 PDF、PDF 原生预览和转换失败提示。
- [x] 评估并移除未使用的收藏、下载日志、复杂角色授权和多版本接口。
- [x] 将 `DocumentViewSet` 的访问权限从宽泛入口收紧为经过认证和对象级校验。
- [x] 同步简化批量导入模板、Django Admin 和 API 返回结构。

### 验收标准

- [x] 仪器 API 中不存在预约和培训相关入口。
- [x] 仪器详情仍可完整查看图片、详细位置和使用说明。
- [x] 内部资料列表、详情、预览、下载、编辑、删除和批量导入均正常。
- [x] 数据迁移前后核心仪器和资料数量一致，文件可正常访问。
- [x] 未登录用户不能访问内部资料及其预览、下载地址。

完成记录（2026-07-16）：迁移前已备份开发数据库；应用 `instruments.0003` 与 `documents.0005` 后保留 41 台仪器、49 份内部资料、49 个原文件、43 个既有 PDF 预览和 3 条下载日志。删除空置的仪器培训、维护、故障、二维码和附加图库模型，资料改为主表单文件结构并统一为已审核成员可见、上传者可维护。完整后端测试 64 项通过，前端生产构建和 Django 系统检查通过。

## 阶段三：拆分超大页面

### 目标

降低单文件复杂度，让门户内容、资料库、学生档案和仪器平台能够独立维护。此阶段以结构拆分为主，不改变业务规则和视觉风格。

### 门户内容

- [x] 将 `PortalCms.vue` 拆为页面容器、左侧栏目导航和各内容编辑模块。
- [x] 按站点首页、横幅、研究方向、团队成员、新闻、论文、项目、专利、获奖拆分组件。
- [x] 列表加载、列表映射、分页和批量导入已提取为 composable，站点设置与各栏目表单状态已完成下沉。
- [x] 各编辑模块独立维护字段、校验、上传进度和保存删除请求；新闻草稿插图与论文引文解析状态不再由页面容器管理。

### 资料库

- [x] 将 `DocumentsHome.vue` 拆为分类导航、资料列表、详情预览、编辑表单和批量导入模块。
- [x] 将列表与详情路由状态分开，查看详情后返回时保留页码、分类和搜索词。
- [x] 将文件转换状态轮询从页面组件中提取。

### 学生档案

- [x] 将 `StudentsHome.vue` 拆为学生列表、档案摘要、导师关系、归档资料列表和上传表单。
- [x] 压缩学生基础信息展示，突出归档资料。
- [x] 将学生搜索、学位筛选、分页和当前学生选择状态独立管理。

### 仪器平台

- [x] 将仪器列表、筛选栏、新建/编辑表单、批量导入和详情操作拆分。
- [x] 修复编辑后错误跳回预览页等路由状态问题。

### 验收标准

- [x] 页面功能、API 请求数量和用户操作路径与拆分前一致。
- [x] 主要页面容器只负责布局和模块协调，不再包含大段重复表单逻辑。
- [x] 子模块具备清晰的 props、emits 和类型定义。
- [x] 详情返回列表后保留原页码和筛选条件；滚动位置统一由路由与返回组件记录并在异步列表渲染后恢复。
- [x] 前端生产构建通过，无 TypeScript 错误。

## 阶段四：建立统一分页、上传、预览和文件组件

### 目标

统一重复交互，避免每个页面分别实现一套分页、上传和文件预览逻辑。

### 任务

- [x] 新建统一分页组件：上一页、页码跳转、下一页，窄侧栏和手机端均可用。
- [x] 统一页码参数、页大小和总数解析；删除“12 人/页”等不必要的孤立说明。
- [x] 建立列表查询状态 composable，统一搜索防抖、翻页和删除最后一项后的页码回退。
- [x] 已建立拖拽/选择、分类大小校验、文件名、文件大小、移除和进度组件，并支持取消正在进行的网络上传。
- [x] 保留批量导入与普通文件上传的不同提示，但复用底层进度逻辑。
- [x] 新建文件展示组件，统一文件图标、名称截断、大小、类型和操作菜单。
- [x] 新建预览组件，统一 PDF、图片、视频、Word/PPT 转换结果和无法预览状态。
- [x] 防止重复进度条，已迁移页面只展示组件提供的一处进度反馈。
- [x] CMS、资料库、学生档案、仪器和成员页面已统一加载失败重试与请求错误文案。
- [x] 将门户附件、新闻 Word、内部资料、学生档案、仪器图片和成果附件迁移到公共上传、进度或文件展示组件。

### 验收标准

- [x] 所有列表的分页样式和行为一致，并可直接跳转页码。
- [x] 删除当前页最后一项后自动回到有效页，不出现空白列表。
- [x] 所有文件上传均显示真实进度；现有文件和新选择文件均显示文件大小。
- [x] 同一个上传任务不会出现两个进度条。
- [x] 文件预览使用动态视口高度，桌面端和手机端均保留足够可视区域。
- [x] 预览、下载和删除按钮使用后端返回的能力字段，后端对象级权限测试覆盖允许与拒绝路径。

完成记录（2026-07-17）：统一加载失败重试已覆盖主要内部页面，图片入口统一显示现有文件大小，文件预览改用桌面/手机动态视口高度。手机端 390px 视口检查无横向溢出；资料与学生权限回归 28 项通过，前端生产构建通过。

## 阶段五：清理首页字段语义与演示内容

### 目标

确保门户内容后台的每个字段都能在公开网站找到明确对应位置，公开页面不再依赖写死文本或无用演示数据。

### 任务

- [x] 建立“后台字段 -> API 字段 -> 前台展示位置”映射表。
- [x] 清理站点首页中未使用、重复或命名含糊的字段。
- [x] 明确站点名称、单位副标题、首页简介、横幅标题、横幅副标题、页脚内容和外链的唯一来源。
- [x] 首页简介只展示可编辑简介，不重复研究方向、单位和地址。
- [x] 研究方向移除关键词字段及前后台残留展示。
- [x] 团队成员只展示排序大于 0 的内容，并使用身份头衔、头像和简短方向。
- [x] 成果排序规则统一：排序值大于 0 时优先按升序；未指定排序时按日期从新到旧。
- [x] 论文、项目、专利和获奖在成果模块分别展示，不再重复统计。
- [x] 新闻分类统一为：学术交流、组内动态、科研进展、成果荣誉、招生招聘、项目动态。
- [x] 删除会生成默认研究方向、成果、新闻、仪器和内部资料的演示种子命令。
- [x] 保留一个质量合格的默认横幅，仅在后台没有有效横幅时使用。
- [x] Logo、图标、横幅、成员头像和附件均使用 API 文件 URL，并通过更新时间参数刷新品牌缓存。

### 验收标准

- [x] 门户首页主要文字、图片和外链均可在“门户内容”中编辑。
- [x] 站点首页与页脚保存负载已拆分，不再互相覆盖未保存字段。
- [x] 页面无重复标题、重复摘要或无法编辑的装饰性文案。
- [x] 清空业务数据后不会重新出现演示成员、论文、项目、新闻或仪器。
- [x] 上传后的图片、PDF 和附件统一使用 API 返回 URL。

完成记录（2026-07-17）：新增门户字段映射文档；`keywords` 迁移为语义明确的 `hero_subtitle`，删除研究方向关键词字段；页脚说明恢复编辑入口，单位不再由前端拼接；首页删除固定模块说明和重复简介引导；新增“项目相关”新闻分类并移除演示数据种子命令。门户、成员与 CMS 测试 11 项通过，首页桌面与手机页面回归通过。

## 阶段六：补齐关键流程和权限测试

### 目标

为前五个阶段建立回归保障，确保后续调整不会再次破坏登录、分页、上传、预览和对象级权限。

### 后端测试

- [x] 账号名和邮箱登录。
- [x] 学校身份、成员状态和系统权限互不覆盖。
- [x] 毕业成员状态迁移、账号保留和手动停用。
- [x] 一个账号只能生成一个学生档案，非学生身份不能误生成档案。
- [x] 学生可关联多个导师。
- [x] 管理员、资料管理员、上传者和普通成员的查看、编辑、删除权限矩阵。
- [x] 未登录用户无法访问内部资料、学生档案和仪器管理 API。
- [x] 文件预览和下载不能绕过对象权限。
- [x] 门户成果服务端搜索分页已覆盖；仪器、内部资料、学生和成员的前端搜索分页已由桌面端和手机端页面自动化覆盖。
- [x] 已覆盖论文 DOI 去重、资料错误行、仪器同名更新、导入事务回滚、Excel 内嵌图片及压缩包附件边界。
- [x] 删除分页末项后，统一分页 composable 会自动回落到最后一个有效页。

### 前端测试

- [x] 登录、退出、会话过期状态清理、受保护路由跳转和原页面返回均已覆盖。
- [x] 成员创建账号、编辑身份/权限、生成学生档案和处理毕业状态。
- [x] 门户内容列表切换、编辑、保存、批量导入及图片预览。
- [x] 新闻 Word 上传、处理完成后展示、切换新闻时状态不串联。
- [x] 内部资料上传进度、预览、下载、编辑和删除。
- [x] 学生档案搜索、筛选、翻页、详情返回和归档资料上传。
- [x] 仪器搜索、分页、新建、编辑、详情和删除。
- [x] 论文、项目、专利、获奖的列表翻页、详情返回和附件查看。
- [x] 全局 Loading 已覆盖快速请求、慢请求和并发请求的出现及结束。
- [x] 桌面端和手机端导航、内部侧栏、表格/列表、分页和预览区域可用。

### 建议验证命令

```bash
docker compose exec backend python manage.py test
(cd frontend && npm run build)
git diff --check
```

如补充前端测试框架，再增加：

```bash
(cd frontend && npm run test)
(cd frontend && npx playwright install chromium)
(cd frontend && npm run test:e2e)
```

### 验收标准

- [x] 后端权限测试覆盖允许和拒绝两类结果，而不只测试成功路径。
- [x] 已建立 Vitest 和 Playwright，覆盖统一分页、会话状态、请求错误、全局 Loading 与页面关键流程。
- [x] 后端测试使用 pytest 隔离数据库并在用例结束后清理。
- [x] 当前后端全量测试、前端生产构建、Django 检查和 `git diff --check` 全部通过。

完成记录（2026-07-17）：补充邮箱登录、停用账号、单账号单档案、多导师、门户字段语义和新闻分类测试；建立 Vitest，覆盖统一分页末项回退、会话状态、请求错误和全局 Loading。建立 Playwright 桌面端/手机端回归，覆盖登录退出、详情返回与滚动恢复、成员账号和学生建档、资料与学生文件上传、仪器操作、门户新闻与成果导入、横幅图片、成果附件及响应式导航。后端补充成果搜索分页、论文 DOI 去重、资料错误行、仪器同名更新、事务回滚、Excel 内嵌图片、新闻 Word 图片及 ZIP 附件测试。后端 86 项、前端 Vitest 12 项、Playwright 19 项执行测试全部通过，3 项按设备项目跳过；生产构建、Django 检查、迁移检查、依赖审计和差异检查通过。

## 推荐推进记录

每完成一个阶段，在此处补充日期、迁移说明、测试结果和提交号。

| 阶段 | 状态 | 完成日期 | 验证结果 | 提交 |
| --- | --- | --- | --- | --- |
| 一、身份权限与毕业成员 | 已完成 | 2026-07-16 | 后端 62 项测试、前端构建、Django 检查通过 |  |
| 二、仪器与内部资料后端 | 已完成 | 2026-07-16 | 后端 64 项测试、前端构建、Django 检查通过 |  |
| 三、超大页面拆分 | 已完成 | 2026-07-16 | 门户、仪器、资料库和学生档案完成拆分；新闻切换、论文长标题点击与表单回填通过页面回归，前端构建通过 |  |
| 四、统一交互组件 | 已完成 | 2026-07-17 | 统一分页、上传、文件行、预览、加载失败重试；权限回归 28 项、手机 390px 验收、前端构建通过 |  |
| 五、首页语义与演示内容 | 已完成 | 2026-07-17 | 字段映射、语义迁移、首页去重、页脚拆分、新闻分类和演示种子清理完成；相关后端测试 11 项与页面回归通过 |  |
| 六、关键流程与权限测试 | 已完成 | 2026-07-17 | 后端 86 项、Vitest 12 项、Playwright 19 项通过；桌面/手机关键流程、导入附件边界、构建与安全审计完成 |  |
